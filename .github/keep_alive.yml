name: Render Ping

on:
  schedule:
    - cron: '*/14 * * * *'
  workflow_dispatch:

jobs:
  ping_at:
    runs-on: ubuntu-latest
    steps:
      - name: Siteyi Dürt
        run: |
          curl -A "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/148.0.0.0 Safari/537.36" https://https://yorum-analiz.onrender.com/
